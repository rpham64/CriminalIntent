<?xml version="1.0" encoding="utf-8"?>
<manifest package="rpham64.criminalintent"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.WRITE_CONTACTS"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <permission android:name="android.permission.INTERACT_ACROSS_USERS_FULL"
                android:protectionLevel="signature"
        />

    <uses-permission android:name="android.permission.INTERACT_ACROSS_USERS_FULL" android:protectionLevel="signature" />

    <!-- camera for API 20 and below -->
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <!-- camera2 for API 21+ only -->
    <!--<uses-feature android:name="android.hardware.camera2"-->
                  <!--android:required="false"/>-->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="rpham64.criminalintent.ui.list.CrimeListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name="rpham64.criminalintent.ui.pages.CrimePagerActivity"
            android:label="@string/app_name"
            android:parentActivityName="rpham64.criminalintent.ui.list.CrimeListActivity"
            />
        <activity
            android:name="rpham64.criminalintent.ui.AboutActivity"
            android:label="@string/about_my_app"
            android:parentActivityName="rpham64.criminalintent.ui.list.CrimeListActivity"
            android:theme="@style/AboutLibrariesTheme"
            />
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths"/>
        </provider>
    </application>

</manifest>
